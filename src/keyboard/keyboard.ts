import { Button } from './button';

export const Keyboard = {
    gameStarted: [
        [Button.Stop, Button.Settings, Button.Info, Button.Exit],
    ],
    gameNotStarted: [
        [Button.Start, Button.Settings, Button.Info, Button.Exit],
    ],
}